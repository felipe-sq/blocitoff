<div class="row">
  <div class="col-md-8">
    <% @user.items.each do |item| %>
    <ul>
      <font size="4.5"><%= item.name %></font>
      <%= link_to "", item, method: :delete, class: 'glyphicon glyphicon-ok' %>
    </ul>
    <% end %>
  </div>
</div>
