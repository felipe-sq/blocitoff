<div class="row">
  <div class="col-md-8">
    <% @user.items.each do |item| %>
      <div class='media', id="item-<%=item.id %>">
        <font size="4.5"><%= item.name %></font><%= link_to "", item, method: :delete, remote: true, class: 'glyphicon glyphicon-ok' %>
      </div>
    <% end %>
  </div>
</div>
